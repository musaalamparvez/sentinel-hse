---
description: Review corrections made this session and update AGENTS.md/CLAUDE.md/_docs if anything is now stale
---

Before doing anything else: if there are uncommitted code changes, commit them now (ask first if unclear what should go in the commit message). Doc updates must be a separate, later commit.

Then review this session for corrections I made to your approach, output, or understanding — mistakes you fixed, conventions you learned, gotchas you hit.

For each correction, check `AGENTS.md`, `CLAUDE.md`, and `_docs/*.md` for a doc it should update. Only touch a doc if the correction reveals something a future session genuinely could not infer from reading the code — skip anything that's just restating good practice, generic advice, or config already visible in `pyproject.toml`/`package.json`/settings files. Prefer trimming a doc over padding it.

If nothing from this session clears that bar, say so and don't touch any doc.

If something does, show me the proposed diff before committing it.
